import css from "./PlayListComponent.module.css";

const PlayListComponent = (props) => {
  const { title, elements, seeMore } = props;
  return <div>PlayListComponent</div>;
};

export default PlayListComponent;
